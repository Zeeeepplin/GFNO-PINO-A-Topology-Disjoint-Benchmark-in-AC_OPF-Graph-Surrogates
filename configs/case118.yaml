case: case118
seed: 2026
output_dir: data/processed
include_base_topology: true
scenarios_per_topology: 100
run_dc_baseline: true
load_perturbation: {min: 0.60, max: 1.40, correlation_length: 4.0}
renewable: {capacity_fraction: 0.25, csv_path: null}
split: {fractions: [0.70, 0.15, 0.15]}
solver: {backend: pandapower}
model: {bus_channels: 8, edge_channels: 10, width: 160, depth: 8, chebyshev_order: 5, dropout: 0.05}
training: {batch_size: 16, warm_start_epochs: 120, physics_epochs: 250, learning_rate: 0.0008, weight_decay: 0.00001, grad_clip: 1.0, num_workers: 0, seed: 2026}
