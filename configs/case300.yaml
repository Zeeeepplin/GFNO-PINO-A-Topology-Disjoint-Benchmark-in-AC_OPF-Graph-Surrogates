case: case300
seed: 2026
output_dir: data/processed
include_base_topology: true
scenarios_per_topology: 80
run_dc_baseline: true
load_perturbation: {min: 0.50, max: 1.50, correlation_length: 5.0}
renewable: {capacity_fraction: 0.30, csv_path: null}
split: {fractions: [0.70, 0.15, 0.15]}
solver: {backend: pandapower}
model: {bus_channels: 8, edge_channels: 10, width: 192, depth: 8, chebyshev_order: 5, dropout: 0.05}
training: {batch_size: 8, warm_start_epochs: 150, physics_epochs: 300, learning_rate: 0.0005, weight_decay: 0.00001, grad_clip: 1.0, num_workers: 0, seed: 2026}
