case: case57
seed: 2026
output_dir: data/processed
include_base_topology: true
scenarios_per_topology: 100
run_dc_baseline: true
load_perturbation: {min: 0.65, max: 1.35, correlation_length: 3.5}
renewable: {capacity_fraction: 0.20, csv_path: null}
split: {fractions: [0.70, 0.15, 0.15]}
solver: {backend: pandapower}
model: {bus_channels: 8, edge_channels: 10, width: 128, depth: 6, chebyshev_order: 4, dropout: 0.05}
training: {batch_size: 24, warm_start_epochs: 100, physics_epochs: 200, learning_rate: 0.001, weight_decay: 0.00001, grad_clip: 1.0, num_workers: 0, seed: 2026}
